/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package projectbangundatar2;

/**
 *
 * @author HP
 */
public class PersegiPanjang {
    
    int keliling(int panjang, int lebar){
        return (2 * (panjang + lebar));
    }
    
    double keliling(double panjang, double lebar) {
        return (2 * (panjang + lebar));
    }
    
    double keliling(double panjang, int lebar) {
        return (2 * (panjang + lebar));
    }
    
    double keliling(int panjang, double lebar) {
        return (2 * (panjang + lebar));
    }

    int luas(int panjang, int lebar){
        return (panjang * lebar);
    }
    
    double luas(double panjang, double lebar) {
        return (panjang * lebar);
    }
    
    double luas(double panjang, int lebar) {
        return (panjang * lebar);
    }
    
    double luas(int panjang, double lebar) {
        return (panjang * lebar);
    }

}
